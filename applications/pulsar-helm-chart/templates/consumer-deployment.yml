apiVersion: apps/v1
kind: Deployment
metadata:
  name: pulsar-consumer
  labels:
    app: consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumer
  template:
    metadata:
      labels:
        app: consumer
    spec:
      containers:
      - name: consumer
        image: abhishekfk/pconsumer
        ports:
        - containerPort: 80
      topics:
